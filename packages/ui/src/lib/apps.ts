/**
 * ID of the pre-installed Terminal app. Mirrors tomod's DEFAULT_TEMPLATE_ID
 * (slugify("Terminal") === "terminal"). It's opened from the dock modal and
 * hidden from the desktop grid.
 */
export const TERMINAL_APP_ID = "terminal";
